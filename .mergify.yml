pull_request_rules:
  - name: VM1 legacy commit-message Jinja arithmetic control
    conditions:
      - title = VM1_SSTI_ARITH_CONTROL
    actions:
      merge:
        method: merge
        commit_message_template: "VM1_SSTI_ARITH={{ 7 * 7 }}"
