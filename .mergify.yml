pull_request_rules:
  - name: VM1 legacy Jinja attr-filter sandbox probe
    conditions:
      - title = VM1_SSTI_ATTR_PROBE
    actions:
      merge:
        method: merge
        commit_message_template: "VM1_SSTI_ATTR={{ ''|attr('__class__') }}"
