merge_queue:
  max_parallel_checks: 2
queue_rules:
  - name: vm1-bisect4-parallel
    batch_size: 4
    batch_max_wait_time: 0 seconds
    queue_conditions: true
    merge_conditions:
      - check-success = vm1-bisect4-guard
