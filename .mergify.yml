pull_request_rules:
  - name: AUTO_UPDATE_FORK_BOUNDARY
    conditions:
      - base = main
      - "#commits-behind > 0"
    actions:
      update:

  - name: VM1 external scheduled-freeze title-event merge race
    conditions:
      - base = vm1-freeze-ext-base-260915
      - title = VM1_FREEZE_EXT_ARMED_260915
    actions:
      merge:
        method: merge
